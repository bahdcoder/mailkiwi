import { FastifyReply, FastifyRequest } from "fastify";
import { inject } from "tsyringe";
<% if (actionExists) { %>
import { <%= pascalCaseName %>Action } from "@/domains/<%= domain %>/actions/<%= snakeCaseName %>_action.js";
<% } %>
<% if (repositoryExists) { %>
import { <%= repositoryNamePascalCase %>Repository } from "@/domains/<%= domain %>/repositories/<%= repositoryName %>_repository.js";
<% } %>

export class <%= pascalCaseName %>Controller {
<% if (repositoryExists) { %>
  constructor(
    @inject(<%= repositoryNamePascalCase %>Repository)
    private <%= repositoryName.toLowerCase() %>Repository: <%= repositoryNamePascalCase %>Repository
  ) {}
<% } %>

  async find(request: FastifyRequest, reply: FastifyReply) {
    // Implement controller logic here
  }

  async create(request: FastifyRequest, reply: FastifyReply) {
    // Implement controller logic here
  }

  async update(request: FastifyRequest, reply: FastifyReply) {
    // Implement controller logic here
  }

  async delete(request: FastifyRequest, reply: FastifyReply) {
    // Implement controller logic here
  }
}
