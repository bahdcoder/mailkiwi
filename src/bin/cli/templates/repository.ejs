import { inject, injectable } from "tsyringe";
import { PrismaClient } from "@prisma/client";
import { ContainerKey } from "@/infrastructure/container";
import { <%= pascalCaseName %>Dto } from "../dto/<%= snakeCaseName %>_dto";

@injectable()
export class <%= pascalCaseName %>Repository {
  constructor(
    @inject(ContainerKey.database)
    private database: PrismaClient
  ) {}

  async find() {
    // Implement repository logic here
  }

  async create(payload: <%= pascalCaseName %>Dto) {
    // Implement repository logic here
  }

  async update(payload: <%= pascalCaseName %>Dto, id: string) {
    // Implement repository logic here
  }

  async delete(id: string) {
    // Implement repository logic here
  }
}
