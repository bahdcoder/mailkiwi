import { inject, injectable } from "tsyringe";
<% if (repositoryExists) { %>
import { <%= repositoryNamePascalCase %>Repository } from "@/domains/<%= domain %>/repositories/<%= repositoryName %>_repository.js";
<% } %>
<% if (generateDto) { %>
import { <%= pascalCaseName %>Dto } from "@/domains/<%= domain %>/dto/<%= snakeCaseName %>_dto.js";
<% } %>

@injectable()
export class <%= pascalCaseName %>Action {
<% if (repositoryExists) { %>
  constructor(
    @inject(<%= repositoryNamePascalCase %>Repository)
    private <%= repositoryName.toLowerCase() %>Repository: <%= repositoryNamePascalCase %>Repository
  ) {}
<% } %>

<% if (generateDto) { %>
  handle = async (payload: <%= pascalCaseName %>Dto) => {
    // Implement action logic here
  };
<% } else { %>
  handle = async () => {
    // Implement action logic here
  };
<% } %>
}
