import { FastifyReply, FastifyRequest } from "fastify";
import { inject } from "tsyringe";
<% if (actionExists) { %>
import { <%= pascalCaseName %>Action } from "@/domains/<%= domain %>/actions/<%= snakeCaseName %>_action";
<% } %>

export class <%= pascalCaseName %>Controller {
<% if (actionExists) { %>
  constructor(
    @inject(<%= pascalCaseName %>Action)
    private <%= camelCaseName %>Action: <%= pascalCaseName %>Action
  ) {}
<% } %>

  async find(request: FastifyRequest, reply: FastifyReply) {
    // Implement controller logic here
  }

  async create(request: FastifyRequest, reply: FastifyReply) {
    // Implement controller logic here
  }

  async update(request: FastifyRequest, reply: FastifyReply) {
    // Implement controller logic here
  }

  async delete(request: FastifyRequest, reply: FastifyReply) {
    // Implement controller logic here
  }
}
