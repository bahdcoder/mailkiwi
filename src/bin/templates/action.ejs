import { inject, injectable } from "tsyringe";
<% if (repositoryExists) { %>
import { <%= repositoryName %>Repository } from "@/domains/<%= domain %>/repositories/<%= repositoryName %>_repository";
<% } %>
<% if (generateDto) { %>
import { <%= pascalCaseName %>Dto } from "@/domains/<%= domain %>/dto/<%= snakeCaseName %>_dto";
<% } %>

@injectable()
export class <%= pascalCaseName %>Action {
<% if (repositoryExists) { %>
  constructor(
    @inject(<%= repositoryName %>Repository)
    private <%= repositoryName.toLowerCase() %>Repository: <%= repositoryName %>Repository
  ) {}
<% } %>

<% if (generateDto) { %>
  handle = async (payload: <%= pascalCaseName %>Dto) => {
    // Implement action logic here
  };
<% } else { %>
  handle = async () => {
    // Implement action logic here
  };
<% } %>
}
